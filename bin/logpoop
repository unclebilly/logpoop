#!/usr/bin/env ruby
$:.unshift File.join(File.dirname(__FILE__), "..", "lib")
require 'logpoop'

if(['help', '-h', '--help'].include?(ARGV[0]))
  puts LogPoop.usage
else
  LogPoop.new(ARGV[0], ARGV[1])
end
